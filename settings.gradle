pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            //Forge
            url = 'https://maven.minecraftforge.net/'
        }
        maven {
            //Parchment
            url = 'https://maven.parchmentmc.org'
        }
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.5.0'
}

rootProject.name = 'Mythril' //Should match root directory name
